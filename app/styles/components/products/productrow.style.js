import { StyleSheet } from "react-native";
import { COLORS, SIZES } from "../../../constants/index";

export default productRowStyles = StyleSheet.create({
  rowContainer: {
    marginTop: SIZES.medium,
    marginHorizontal: SIZES.marginHorizontal,
  },
});
